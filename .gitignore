# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
symliv-mcp.log

# Holds environment URLs, JWT secrets, and cached admin tokens — never commit.
src/profiles.json
